<%= form_with model: page1_post, local: true , url: page1_posts_path, html: { multipart: true } do |form| %>
  <% if page1_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(page1_post.errors.count, "error") %> prohibited this page1_post from being saved:</h2>

      <ul>
      <% page1_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
